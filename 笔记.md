# 我的个人笔记

这个仓库是我跟着 AI 从零学会 Git 的真实记录。
每学一步,就在这里加一条,慢慢攒成自己的知识库。

---

## Git 四区模型(代码流向)

工作区 → 暂存区 → 本地仓库 → 远程仓库(GitHub)

- 工作区 / 暂存区 / 本地仓库:都在**我的电脑**上
- 远程仓库:在 **GitHub 服务器**上,只有 push / pull 需要联网

## 日常命令循环

```bash
git switch main          # 切回主分支
git pull                 # 拉取 GitHub 最新
git switch -c feature/xxx  # 新建并切换到功能分支
# ...改文件...
git add .                # 放进暂存区
git commit -m "说明"     # 提交到本地历史
git push -u origin feature/xxx  # 推到 GitHub
# 然后去网页发 Pull Request,审查后合并
```

## git rebase 简记

`git rebase` 把你的分支"嫁接"到目标分支最新位置,让历史变成一条直线。
- 和 `merge` 的区别:`merge` 会留一个合并提交,历史像分叉;`rebase` 把分叉拉直。
- 用在哪:把自己的 feature 分支更新到 main 最新,再合并,历史更干净。
- 注意:**别对已经 push 出去、别人在用的分支做 rebase**(会改写历史)。

## 待补充

- [ ] 合并冲突怎么解决
- [x] git rebase 是什么(见上方)
- [ ] 怎么撤销一次提交

---

## 第 1 阶段学习记录:Git 基础与核心原理

### 四区模型(改动流向)

- 工作区 Working Tree:看得见、能直接编辑的文件
- 暂存区 Staging Area:下次要提交的清单(`git add` 把改动放进来)
- 仓库 Repository:一串不可改的 commit(`git commit` 把暂存区固化成历史)
- 远程 Remote:GitHub 上的副本(`git push` 推过去 / `git pull` 拉回来)

### 关键概念

- HEAD:指向"你现在站在哪个 commit 上"
- Branch:一串 commit 的名字标签,`main` 是默认主分支
- Commit:暂存区那一刻的快照,一旦提交就不可改

### 已掌握命令

- `git status`:看四区当前状态
- `git restore <file>`:从历史把误删/误改的文件救回工作区
